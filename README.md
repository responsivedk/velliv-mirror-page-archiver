# Velliv - AC mirror page archiver

## Installation

Run `npm install`

## Usage

Run `npm index.js --file [file name]`

## Data file format

CSV with headers:

`BroadLogId,DeliveryId,MirrorPageUrl,BroadLogMsgId`
